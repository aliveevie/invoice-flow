// Defaults used across the transfer flow. Override via function params when needed.

export const DEFAULT_MAX_FEE = 500n; // 0.0005 USDC (6 decimals)
export const DEFAULT_MIN_FINALITY_THRESHOLD_FAST = 1000; // <= 1000 for fast transfers
export const DEFAULT_ATTESTATION_POLL_INTERVAL_MS = 5000;
export const USDC_DECIMALS = 6;


